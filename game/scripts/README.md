# Game Scripts

Utility scripts for browser build preparation and validation.

- `prepare-public.mjs` copies shared game JSON and normalized Pixel Quest assets into `game/public/`.
- `check-public.mjs` fails the build early when required generated public files are missing.

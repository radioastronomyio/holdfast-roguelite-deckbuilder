## 1. Project Setup

- [ ] 1.1 Create directory structure: `data/cards/`, `data/entities/`, `data/campaign/`, `simulation/models/`, `simulation/tests/`, `simulation/tests/fixtures/valid/`, `simulation/tests/fixtures/invalid/`. Add `__init__.py` to `simulation/`, `simulation/models/`, `simulation/tests/`. Create `data/enums.json` as empty placeholder. (Verify: all directories exist, Python packages importable)
- [ ] 1.2 Create `simulation/requirements.txt` with `pydantic>=2.0` and `pytest>=7.0`. (Verify: `pip install -r simulation/requirements.txt` succeeds)

## 2. Modifier Schema (GDD: "Core Design Principle", "The Modifier Tuple", "Resolution Order")

- [ ] 2.1 Create `simulation/models/enums.py` defining StrEnum classes: `Stat` (HP, Power, Speed, Defense, Energy), `Operation` (FLAT_ADD, FLAT_SUB, PCT_ADD, PCT_SUB, MULTIPLY), `Target` (SELF, ALLY_SINGLE, ALLY_ALL, ENEMY_SINGLE, ENEMY_ALL, GLOBAL), `Stacking` (stack, replace, max), `AiHeuristic` (aggressive, defensive, balanced), `NarrativePosition` (approach, settlement, stronghold), `EncounterType` (combat, hazard, event). (Verify: all enums importable, string serialization works)
- [ ] 2.2 Create `simulation/models/modifier.py` defining `Modifier` Pydantic model with fields: stat (Stat), operation (Operation), value (int — integers only, no floats), duration (int, validated: >= -1), target (Target), stacking (Stacking, default "replace"), tags (list[str], default []). Add validator: duration must be >= -1 (reject -2 and below). The tags field carries no logic in v1 — it is stored and passed through for future condition evaluation and synergy systems. Define `STAT_SCALE` as a module-level constant (initial value TBD — set to a power of 10 large enough that no percentage operation produces a fractional result; increase if M2 simulation surfaces precision issues). All base stats in entity definitions are stored pre-scaled (raw value × STAT_SCALE). (Verify: valid modifier constructs, float value rejected, invalid duration raises ValidationError, tags defaults to empty list)
- [ ] 2.3 Create `data/enums.json` listing all enum values for reference by non-Python consumers. (Verify: JSON is valid, contains all enum names and values)
- [ ] 2.4 Create `simulation/tests/fixtures/valid/modifiers.json` with at least 5 valid modifiers covering: instant (duration 0), permanent (duration -1), turn-based (duration 3), each stacking type, and various stat/operation combos. Create `simulation/tests/fixtures/invalid/modifiers.json` with at least 4 invalid modifiers: missing field, bad enum value, duration -2, wrong type. (Verify: valid fixtures parse as Modifier, invalid fixtures raise ValidationError)
- [ ] 2.5 Create `simulation/tests/test_modifier.py` with parametrized tests loading valid and invalid fixtures. Include resolution order documentation test: assert that the formula `(base + flat_sum) * (100 + pct_sum) / 100` then sequential multiply matches expected outputs for test vectors: (base=100, FLAT_ADD 20, PCT_ADD 50 → 180), (base=0, PCT_ADD 50 → 0), (base=100, FLAT_ADD 10, PCT_ADD 20, MULTIPLY 1.5 → 198). Note: the resolution function itself is M2 scope, but the test vectors live here as fixtures for M2 to consume. (Verify: `pytest simulation/tests/test_modifier.py` passes)

## 3. Card Schema (GDD: "Card System", "Card Upgrade System")

- [ ] 3.1 Create `simulation/models/card.py` defining: `UpgradeEntry` model (added_effects: list[Modifier], prerequisite: str or None, tier: int 1-3, exclusions: list[str]), `UpgradeTree` as dict[str, UpgradeEntry], `Card` model (id: str, name: str, energy_cost: int >= 0, effects: list[Modifier], tags: list[str], upgrade_tier: int 0-3, upgrade_paths: UpgradeTree). Add validators: energy_cost >= 0, tier 1 entries must have prerequisite None, tier 2-3 entries must have non-None prerequisite. (Verify: valid card constructs, negative energy_cost rejected, prerequisite rules enforced)
- [ ] 3.2 Create `data/cards/base-cards.json` with 10-15 base cards from GDD: Arcane Strike, Immolate, Shield Bash, Sweeping Blade, Phalanx, Adrenaline, Cleanse, Deep Focus, Acid Flask, plus 1-6 additional cards to reach the target count. Each card at tier 0 with effects as modifier tuples matching GDD values. (Verify: all cards parse as Card model, all effects are valid modifiers)
- [ ] 3.3 Create `data/cards/upgrade-trees.json` mapping each base card ID to its full upgrade tree (tiers 1-3, 2 branches per tier). Follow the GDD Strike example for structure. Ensure no branch pits economy manipulation against flat damage. (Verify: all trees parse as UpgradeTree, prerequisite chains valid, exclusion references valid)
- [ ] 3.4 Create `data/cards/hazard-cards.json` with Tripwire and Miasma from GDD, plus 3-5 additional hazard cards. These are region-played, not in the player pool. (Verify: all hazard cards parse as Card model, energy_cost is 0 for all)
- [ ] 3.5 Create `simulation/tests/fixtures/valid/cards.json` and `simulation/tests/fixtures/invalid/cards.json`. Invalid cases: missing effects, negative energy_cost, tier 2 with null prerequisite, upgrade tree referencing nonexistent branch. (Verify: valid pass, invalid raise ValidationError)
- [ ] 3.6 Create `simulation/tests/test_card.py` loading fixtures plus cross-validating `data/cards/base-cards.json` and `data/cards/upgrade-trees.json`. Test: all base cards valid, all upgrade trees valid, prerequisite chains intact, no economy-vs-damage branches (document as advisory check with clear assertion message). (Verify: `pytest simulation/tests/test_card.py` passes)

## 4. Entity Schema (GDD: "Character System", "Enemy Design")

- [ ] 4.1 Create `simulation/models/entity.py` defining: `CharacterGenerationBounds` model (per-stat min/max dicts, total_budget_min: int, total_budget_max: int), `Character` model (id: str, name: str, base_stats: dict[Stat, int] — all 5 required, innate_passive: Modifier — must have duration -1, name_parts: dict with first_name/title/origin strings), `Enemy` model (id: str, name: str, base_stats: dict[Stat, int] — all 5 required, card_pool: list[str] — min length 1, ai_heuristic_tag: AiHeuristic, is_elite: bool default False). (Verify: valid entities construct, missing stat rejected, non-permanent passive rejected, empty card_pool rejected)
- [ ] 4.2 Create `data/entities/example-characters.json` with GDD examples: Vanguard Sentinel (140/12/80/20/3, +15% Defense permanent), Ember Mage (65/28/115/5/4, +20% Power conditional), Field Tactician (85/15/105/12/5, +1 Energy permanent). The Ember Mage's conditional passive (vs enemies below 50% HP) is a tagged modifier: store as a standard `PCT_ADD 20 power` modifier with `tags: ["conditional_hp_threshold_50"]`. The resolver in M2 reads the tag and applies the condition — no optional `condition` field on the schema. This is the pattern for all conditional effects. All stat values must be stored pre-scaled (multiply by STAT_SCALE). GDD values are the human-readable display form. The UI layer divides by STAT_SCALE for display — store scaled, show unscaled. (Verify: all parse as Character)
- [ ] 4.3 Create `data/entities/example-enemies.json` with at least 3 enemies: one aggressive (Scavenger Patrol), one defensive, one elite (Warlord Vanguard, Fungal Behemoth). Each with stats, card_pool referencing IDs from base-cards.json, and appropriate ai_heuristic_tag. (Verify: all parse as Enemy, card_pool IDs exist in card data)
- [ ] 4.4 Create `data/entities/generation-bounds.json` defining per-stat min/max and total budget ranges. Bounds MUST encompass all 3 GDD example characters. (Verify: parse as CharacterGenerationBounds, GDD examples fall within bounds)
- [ ] 4.5 Create `simulation/tests/fixtures/valid/characters.json`, `simulation/tests/fixtures/valid/enemies.json`, `simulation/tests/fixtures/invalid/characters.json`, `simulation/tests/fixtures/invalid/enemies.json`. Invalid cases: missing stat, non-permanent passive, empty card_pool, invalid heuristic tag. (Verify: valid pass, invalid fail)
- [ ] 4.6 Create `simulation/tests/test_entity.py` loading fixtures, cross-validating data files, testing generation bounds contain GDD examples. (Verify: `pytest simulation/tests/test_entity.py` passes)

## 5. Flavor & Name Generation Data (GDD: "Flavor & Name Generation System")

- [ ] 5.1 Create directory `mods/default/flavor/`. Add `mods/README.md` documenting the mod architecture: code always loads from `mods/`, `default/` is the base content layer, additional mod directories merge with it. Create `mods/default/README.md` documenting the default mod structure. (Verify: directories exist, both READMEs written)
- [ ] 5.2 Create `mods/default/flavor/given_names.json` with 60+ first names. Mix of fantasy, archaic, and invented. Examples: Mira, Kael, Dusk, Sorn, Veil, Thresh, Lyra, Oswin, Cael, Brynn, Vane, Sable, Drel, Oryn, Mave, Thorn... (Verify: valid JSON array, >= 60 entries)
- [ ] 5.3 Create `mods/default/flavor/archetypes.json` with 20+ character archetype labels. Examples: Mage, Warden, Blade, Wraith, Scout, Sentinel, Arcanist, Brawler, Shade, Lancer, Hex, Rogue, Knight, Channeler, Drifter... (Verify: valid JSON array, >= 20 entries)
- [ ] 5.4 Create `mods/default/flavor/action_verbs.json` with 30+ attack action words. Examples: Surge, Lash, Pulse, Strike, Coil, Rend, Burst, Sear, Crack, Barrage, Crush, Drift, Smite, Rupture, Flare, Cleave, Torrent, Spike, Rift, Shatter... (Verify: valid JSON array, >= 30 entries)
- [ ] 5.5 Create `mods/default/flavor/region_adjectives.json` (30+ entries) and `mods/default/flavor/region_nouns.json` (30+ entries). Adjectives: Ashen, Blighted, Volcanic, Hollow, Sunken, Forsaken, Scorched, Frozen, Fractured, Corroded, Pale, Dread, Silent, Iron, Withered, Tidal, Verdant, Crimson... Nouns: Wastes, Plains, Reach, Hollow, Depths, Summit, Crossing, Vale, Mire, Shelf, Expanse, Crags, Shore, Basin, Passage, Rift, Tangle, Barrens... (Verify: both valid JSON arrays, >= 30 entries each)
- [ ] 5.6 Create `mods/default/flavor/element-stat-map.json` mapping each Stat to default and rare element pools per GDD spec. (Verify: valid JSON, all 5 stats present, each has default and rare arrays)
- [ ] 5.7 Create `mods/default/flavor/epithet-conditions.json` with 20+ epithet condition entries covering all 5 stats, mix of type 1 and type 2 conditions, default and rare pool tiers. Must include GDD examples: the Strong (power >= 70), the Swift (speed >= 80), the Ghost (speed >= 90, rare), the Volatile (power XOR defense, rare). Additional epithets should cover all stats and a variety of operators. Examples: the Enduring (hp >= 120, default), the Frail (hp <= 60, rare), the Tireless (energy >= 5, default), the Unyielding (defense >= 18, default), the Brittle (defense <= 8, rare), the Wrathful (power >= 80, default), the Balanced (type 2, power AND speed both >= 70, rare)... (Verify: valid JSON, >= 20 entries, all 5 stats covered, GDD examples present)
- [ ] 5.8 Create `simulation/models/flavor.py` defining Pydantic models: `EpithetCondition1` (type literal 1, stat: Stat, op: str from enum, value: int), `EpithetCondition2` (type literal 2, stat_a: Stat, op_a: str, value_a: int, logic: Literal["AND","OR","XOR"], stat_b: Stat, op_b: str, value_b: int), `EpithetEntry` (epithet: str, conditions: list[union], pool: Literal["default","rare"]), `ElementStatMap` (per-stat default/rare arrays), `FlavorPools` (simple list wrappers for the remaining pool files). (Verify: all models importable, GDD examples parse correctly)
- [ ] 5.9 Create `simulation/tests/test_flavor.py` loading all flavor files from `mods/default/flavor/` and validating: all pools meet minimum counts, all stat keys present in element-stat-map, all epithet-conditions parse as EpithetEntry, GDD example epithets present by name. (Verify: `pytest simulation/tests/test_flavor.py` passes)

## 6. Campaign Schema (GDD: "Campaign Structure", "Region Design", "World Phase", "Outpost System")

- [ ] 6.1 Create `simulation/models/campaign.py` defining: `ResearchLayer` (level: int 1-4, reveal_type: str, cost: int), `CombatEncounter` / `HazardEncounter` / `EventEncounter` models with shared base fields (type, narrative_position, name, description) plus type-specific fields per spec. `Encounter` as discriminated union on type field. `Region` (id, name, region_type, modifier_stack: list[Modifier], encounters: list[Encounter] — exactly 3, meta_reward: Modifier with duration -1, research_layers: list[ResearchLayer] — exactly 4). `WorldCard` (id, name, upside: list[Modifier] non-empty, downside: list[Modifier] non-empty, description). `OutpostUpgrade` (id, name, description, effects: list[Modifier] all duration -1, cost: int). Add validators: encounters length == 3, encounters[2].type must be "combat", research_layers length == 4, world card upside/downside non-empty, outpost effects all permanent. `EventChoice` (description: str, effects: list[Modifier], cost: list[Modifier] optional). Events must have >= 2 choices. (Verify: valid models construct, all constraints enforced)
- [ ] 6.2 Create `data/campaign/example-regions.json` with 2 GDD regions: Ashen Wastes and Whispering Thicket, each with 3 encounters following the narrative arc, modifier stacks, meta-rewards, and research layers. (Verify: both parse as Region)
- [ ] 6.3 Create `data/campaign/world-deck.json` with all 20 world cards from GDD. Each card with upside and downside modifier arrays matching GDD descriptions. Translate GDD text effects into modifier tuples (e.g., "Forced March: +30% Speed All, -20% HP All" → upside: [{stat: Speed, operation: PCT_ADD, value: 30, target: ALLY_ALL, duration: -1}], downside: [{stat: HP, operation: PCT_SUB, value: 20, target: ALLY_ALL, duration: -1}]). (Verify: all 20 cards parse as WorldCard, all upside/downside non-empty)
- [ ] 6.4 Create `data/campaign/outpost-upgrades.json` with 5 GDD upgrades: Forge (+2 Power all), Watchtower (free research — expressed as modifier with a tag), Infirmary (+10% HP all), War Room (+1 party size — expressed as special modifier or tagged), Library (-25% research cost — expressed as tagged modifier). Note: some outpost effects don't map cleanly to the 5-stat model (party size, research cost). Use a `special_effect` optional string field alongside the modifier array for non-stat effects. (Verify: all parse as OutpostUpgrade, all modifiers permanent)
- [ ] 6.5 Create fixture files: `simulation/tests/fixtures/valid/regions.json`, `valid/world-cards.json`, `valid/outpost-upgrades.json` and corresponding invalid fixtures. Invalid cases: wrong encounter count, non-combat stronghold, empty world card downside, non-permanent outpost modifier, event with 1 choice. (Verify: valid pass, invalid fail)
- [ ] 6.6 Create `simulation/tests/test_campaign.py` loading fixtures, cross-validating data files, testing encounter type discrimination, narrative position constraints. (Verify: `pytest simulation/tests/test_campaign.py` passes)

## 7. Integration Validation

- [ ] 7.1 Create `simulation/tests/test_integration.py` that: loads ALL data files from `data/`, validates every file through its Pydantic model, checks cross-references (enemy card_pool IDs exist in card data, upgrade tree prerequisite chains valid, encounter enemy references exist in enemy data). (Verify: `pytest simulation/tests/test_integration.py` passes)
- [ ] 7.2 Run full test suite: `pytest simulation/tests/ -v`. All tests MUST pass. Fix any failures. (Verify: zero failures, full green output)
- [ ] 7.3 Update `AGENTS.md` Current State from "Pre-Implementation" to "M1 Complete — schemas and data validated". Update `data/README.md` with directory structure and file inventory. (Verify: both files updated accurately)
